generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Item {
  id        Int    @id @default(autoincrement())
  name      String
  type     String
  weight    Int
  rarity    String
  price     Int
  description  String
  createAt     DateTime @default(now())
}