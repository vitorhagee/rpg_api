generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Character {
  id        Int    @id @default(autoincrement())
  name      String
  class     String
  height    Int
  race      String
}